Carcass Django: система для анализа больших данных

Сделано минимально, чтобы можно было быстро поднять проект и наращивать логику анализа.

## Запуск локально

1) Python 3.10+ установлен.
2) Создать окружение и поставить зависимости:
```
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```
3) Применить базовые миграции:
```
python manage.py migrate
```
4) Запустить сервер разработки:
```
python manage.py runserver
```

## Структура
- `manage.py` — точка входа Django.
- `config/` — настройки проекта (settings, urls, wsgi, asgi).
- `analytics/` — приложение с примерами аналитики:
  - `views.py` — главная панель, генерация тестового датасета, простые метрики.
  - `templates/analytics/dashboard.html` — базовый шаблон с графиком и таблицей.
  - `static/analytics/` — стили и JS для дашборда.
- `requirements.txt` — зависимости (Django, pandas для примеров обработки данных).

## Что можно развивать дальше
- Подключить реальные источники данных (CSV, базы, очереди).
- Добавить Celery/Redis для фоновых расчётов.
- Писать данные в отдельный слой хранения (Data Lake / DWH).
- Добавить авторизацию, ролевую модель и аудит событий.
- Набросать API (DRF) для отдачи агрегатов и графиков.

